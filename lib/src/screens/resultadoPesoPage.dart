import 'package:flutter/material.dart';

class ResultadoPesoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Resultado Peso'),
      ),
      body: Center(
        child: Text('Esta es la página de Resultado Peso'),
      ),
    );
  }
}
